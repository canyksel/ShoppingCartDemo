<ul class="nav navbar-nav navbar-right">
    <li>
        @if (Session["CartCounter"] == null)
        {
            @Html.ActionLink("Sepet", "ShoppingCart", "Shopping", new { id = "cartItem" })
        }
        else
        {
            @Html.ActionLink("Sepet(" + Session["CartCounter"]+ ")","ShoppingCart", "Shopping", new { id = "cartItem" })

        }

    </li>
</ul>
